apiVersion: apps/v1
kind: Deployment
spec:
  containers:
    - name: my-service
      args:
        - --registry
        - consul
        - --no-fail-on-initial-config-load
        - --api-key
        - supersecretvalue123
